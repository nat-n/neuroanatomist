Jax.getGlobal().TooltipHelper = Jax.Helper.create
  mouse_moved: (event) ->
    # inform @tooltip of which region is being hovered
    
    
  mouse_clicked: (event) ->
    # if hovering a region then expand the menu for this region