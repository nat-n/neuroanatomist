Jax.getGlobal()['Region'] = Jax.Model.create
  after_initialize: ->
      
      
    
  validate_region: ->
    
  
  cache_region: ->
    
  
  formulate_region: () ->
    # determine exactly which meshes are required
  
  compose: (region) ->
    
  
  stitch: (mesh_data) ->
    